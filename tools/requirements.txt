# Зависимости RAG-части (поиск по даташитам и карточки).
# Браузерная часть не требует вообще ничего.
#
#   python3 -m venv .venv && .venv/bin/pip install -r tools/requirements.txt

pdfplumber>=0.11      # текст + таблицы из PDF — основной парсер
llama-index-core>=0.11  # MarkdownElementNodeParser — структурное чанкование
rank-bm25>=0.2.2      # резервный ранкер, если нет FTS5
reportlab>=4.0        # генератор демо-PDF: нужен для тестов и examples

# Опционально:
# docling>=2.0         # парсер №1 по ТЗ (IBM). Ставится, но веса (~2 ГБ) в
#                      # песочнице недоступны — пайплайн автоматически
#                      # переключается на pdfplumber.
# numpy>=1.24          # плотные векторы (--embed st); импортируется лениво
# sentence-transformers>=3.0  # плотные векторы. Тоже требует весов.
